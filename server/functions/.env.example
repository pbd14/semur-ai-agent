# Placeholder-only local runtime file. Do not put real secrets in Git.
# Production and hosted live integration mode should use Firebase Functions secrets.

NANGO_SECRET_KEY_DEV=
GOOGLE_GENAI_API_KEY=
TELEGRAM_BOT_TOKEN=

# Local emulator defaults used by demo tooling.
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
GCLOUD_PROJECT=semur-ai
