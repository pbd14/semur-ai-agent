# Nango Functions Workspace

This directory is reserved for custom Nango action and sync experiments that can support future Semur integrations.

The primary class project runtime is implemented in `server/functions`. Keep production assistant logic there unless a workflow specifically needs to run inside Nango.
